package Server;

public class Server extends BaseServer{

    public Server(String ip, int port) {
        super();
        setIp(ip);
        setPort(port);
    }

    @Override
    public void run() {

    }
}
